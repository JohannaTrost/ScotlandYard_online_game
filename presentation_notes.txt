réalisations (fonctionnalités implémentées, améliorations éventuelles)

fonctionnalités implémentées

Structure -> mvc pour chaque page (outilsJeu utiliser par les scripts 'vue')
	  -> dossier static pour header, footer, menu 

1. intégration des donnée 

	- utilise un script SQL pour crées tous les tables selon notre model (et supprimer les tables s'ils existent deja)
	 et inserer des donnee depuis la bd database 
	- execution de ce script par un script php qui gere des donnee comme p.ex. les coordonnees et les inserent dans la bd 

2. implementation des differents strategies 

	- basique: dans le controleur de la form pour demarrer les departs des tous detects ... sont initialiser 
		   deplacement aleatoirement des detectives affichage des destinations possibles pour la joueuse et le type de transport de misterX
	- econome: gestion de tickets pour les joueuses par un variable session 
	- pistage: usage et affichage de plus court chemin -> algorithme A*: heuristique -> distances entre quartiers (moyenne de tous coordonnes d'un quartier)

améliorations éventuelles

	Layout -> affichage de area map
	page de victoire -> affichage de detective qui a attraper mister x   


difficultées:

	- décision sur la structure de la bd   -> création du diagramm au debut! 
	- interpretation des exercises         -> "est-ce qu'il faut utiliser le plus court chemin chaque tour?" 
	- desgin des pages avec css et html    -> "pourquoi cette image n'est pas centré ?" "Pourquoi il y a un gap entre le header et le menu ?"
	- Répartition des tâches               -> "si tu continues avec la prochain fonctionnalité c'est plsu efficace!" "Mais comment? j'ai besoin de ton code d'abord"